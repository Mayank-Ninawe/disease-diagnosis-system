# Minimal requirements for deployment test
# Core web application dependencies (stable versions)
Flask==2.0.1
Flask-Cors==3.0.10
gunicorn==20.1.0

# Dependencies explicitly defined to avoid conflicts
Werkzeug==2.0.1
Jinja2==3.0.1
MarkupSafe==2.0.1
itsdangerous==2.0.1
click==8.0.1

# Basic utilities that don't require compilation
requests==2.28.1
python-dotenv==0.19.2
