# Core dependencies only - for initial deployment
Flask==2.0.1
Flask-Cors==3.0.10
gunicorn==20.1.0
Werkzeug==2.0.1
Jinja2==3.0.1
MarkupSafe==2.0.1
itsdangerous==2.0.1
click==8.0.1
setuptools==59.8.0  # Explicitly add setuptools to resolve import issues
wheel==0.37.1       # Required for some packages

# ML dependencies are commented out for initial deployment to avoid build errors
# These will be added back incrementally after initial deployment is successful
# numpy==1.24.0
# scikit-learn==1.3.0
# pandas==2.0.0
